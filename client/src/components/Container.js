import Aside from './Aside';

export default function Container() {

  // In main page I want sidebar with an input and button
  //  Inside that component I want to pull from local storage the list of previous searched cities
  //  I will need a component for a header and Footer
  //  seperate components for current weather and forecast
  //  Inside of forecast component I want components that are cards for the forecast

  return (
    <>
      This will contain all things on webpage.
      <Aside />
    </>
  )
}